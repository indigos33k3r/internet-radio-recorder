{
	title = 'Zündfunk',
	subtitle = 'Die Klangtapete fuer Pop Opas.',
	episodes_to_keep = 28,
	match = function(meta)
		-- lua string match/find: http://www.lua.org/manual/5.1/manual.html#5.4.1
		local lo_ti = meta.DC_title:lower()
		if lo_ti:find('zündfunk') then return true end
		if lo_ti:find('nachtmix') then return true end
		return false
	end,
}